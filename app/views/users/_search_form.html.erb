<%= form_with(url: search_users_path, method: :get, local: true) do |f| %>
  <%= f.text_field :query, placeholder: "Search Users", class: 'search-box' %>
  <%= button_tag type: 'submit', class: 'search-button' do %>
    <i class="fas fa-search"></i>
  <% end %>
<% end %>