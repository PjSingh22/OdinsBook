<p class='right-bar-title friends-right-bar'>Friends</p>
<% friends = current_user.friends.order("Random()").limit(3) %>

<% friends.each do |user|  %>
  <div class="friend-choices">
    <div class='avatar-name'>
      <span class='friend-sug friend-avatar'><%= image_tag user.avatar_thumbnail, class: 'avatar friend_avatar' %></span>
      <span class='friend-sug friend-name'><%= link_to user.name, user_path(user) %></span>
    </div>
    <div class='add-button'>
      <span class='friend-sug friend-add'><%= link_to '-', friends_destroy_path(friend: user.id), method: :delete %> </span>
    </div>
  </div>
<% end %>